AI_GATEWAY_API_KEY=xxx
AI_GATEWAY_BASE_URL=xxxx
AI_GATEWAY_MODEL=@makers/deepseek-v4-flash
WEB_DEV_AGENT_DEBUG=false
# Optional master token. It stays in the Agent Runtime and only mints short-lived
# per-project tenant tokens into direct sandbox CLI calls.
EDGEONE_PAGES_API_TOKEN=
MAKERS_SUB_TOKEN_TTL_SECONDS=3600
# Leave unset. Preview and deploy then use a project derived from the
# conversation, so later turns reach the same site and two users never collide
# over one name. Setting it pins every conversation to the project named here.
# MAKERS_DEPLOY_PROJECT_NAME=
# Which Makers deployment the token belongs to: prod (default), pre, or test.
# It drives both the token issuer and the sandbox CLI, so leaving it unset while
# holding a non-production token makes the sandbox reject its own credential.
MAKERS_API_ENV=prod
# Region of that deployment: china (default) or global. Only read when
# MAKERS_API_ENV is not prod, where hosts cannot be auto-detected.
MAKERS_API_REGION=china
